export enum TxType {
  Debit = 'debit',
  Credit = 'credit',
}

export enum TxStatus {
  Pending = 'pending',
  Completed = 'completed',
  Failed = 'failed',
}

export const ALL_TYPES_OPTION = 'all';
export const ALL_STATUS_OPTION = 'all';
